import { CommandParser } from "./util";

// describe('Parsing Req for obtain commmands', () => {
//    it('It should return a object with the command and the args', () => {

//        const req = { body:{command:"add arg arg"}};
//        const req1 = { body:{command:"follow arg "}};
//        const req2 = { body:{command:"login arg arg arg"}};

//        expect(CommandParser(req)).toEqual({command:"add",data:["arg","arg"]});
//        expect(CommandParser(req1)).toEqual({command:"follow",data:["arg"]});
//        expect(CommandParser(req2)).toEqual({command:"login",data:["arg","arg","arg"]});
       
//    });
   

// });

describe('Matching Operations', () => {
    it('It should return a ', () => {
        
    });
    
});
